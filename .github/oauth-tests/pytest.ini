[pytest]
addopts = -ra
markers =
    rfc(number, section): normative source for a test
    optional_extension(name): extension is tested when supported/required
    advisory: security best-practice test which does not gate baseline conformance
