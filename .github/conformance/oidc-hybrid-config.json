{
  "alias": "${CONFORMANCE_ALIAS}-hybrid",
  "description": "Nextcloud OIDC GitHub Actions hybrid conformance",
  "publish": "no",
  "server": {
    "discoveryUrl": "${NEXTCLOUD_BASE_URL}/.well-known/openid-configuration"
  },
  "client": {
    "client_id": "${OIDC_HYBRID_CLIENT_ID_1}",
    "client_secret": "${OIDC_HYBRID_CLIENT_SECRET_1}"
  },
  "client_secret_post": {
    "client_id": "${OIDC_HYBRID_CLIENT_ID_1}",
    "client_secret": "${OIDC_HYBRID_CLIENT_SECRET_1}"
  },
  "client2": {
    "client_id": "${OIDC_HYBRID_CLIENT_ID_2}",
    "client_secret": "${OIDC_HYBRID_CLIENT_SECRET_2}"
  }
}
