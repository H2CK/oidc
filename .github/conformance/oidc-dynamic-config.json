{
  "alias": "${CONFORMANCE_ALIAS}-dynamic",
  "description": "Nextcloud OIDC GitHub Actions dynamic registration conformance",
  "publish": "no",
  "server": {
    "discoveryUrl": "${NEXTCLOUD_BASE_URL}/.well-known/openid-configuration"
  }
}
